ansible@ansible-controller:~$ cat user.yaml
---
- hosts: all
  become: true
  tasks:
  - name: create user
    user:
      name: "{{item}}"
      create_home: yes
    with_items:
    - liikhitha
    - hanu
# if u want to run 
#ansible-playbook -i inv user.yaml --syntax-check
#ansible-playbook -i inv user.yaml --check
#ansible-playbook -i inv user.yaml 

